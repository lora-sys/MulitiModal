# 主模型 vs 中医模型: 架构分析与必要性评估

## 1. 职责分离

### 1.1 中医模型 (TCM Encoder) - "静态诊断专家"

**职责**: 基于 4 维静态体征 → 9 维体质识别

```python
输入: [舌象, 苔象, 脉象, 面象] = [B, 4]
  ↓
FT-Transformer (预训练, 冻结)
  ↓
输出:
  ├─ tcm_probs: [B, 9] (9种体质概率分布)
  └─ tcm_internal: [B, 128] (高维表征)
```

**核心能力**:
- ✅ 静态体征 → 体质分类 (分类任务)
- ✅ 预训练知识迁移 (大规模中医数据)
- ✅ 不可微的先验知识 (历史知识编码)

**局限性**:
- ❌ 无法感知实时生理状态
- ❌ 无法响应动态变化
- ❌ 无法量化当前放松程度

---

### 1.2 主模型 (DualGatingModel) - "动态融合决策者"

**职责**: 整合动态信号 + 静态诊断 → 实时决策

```python
输入:
  ├─ 动态信号: ECG + EDA = [B, 2, 1000]
  └─ 静态体征: [舌/苔/脉/面] = [B, 4]

  ↓
┌─────────────────────────────────────────┐
│  动态编码器 (ResNet1D)                  │
│  ECG + EDA → 128D 动态表征              │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│  TCM Encoder (辅助)                      │
│  静态体征 → 128D + 9D 体质概率          │
└─────────────────────────────────────────┘
  ↓
┌─────────────────────────────────────────┐
│  Gate A: TCM 概率 → 调制动态信号        │
│  Gate B: 动态信号 → 调制静态特征        │
└─────────────────────────────────────────┘
  ↓
融合: [128D ⊕ 128D] → [256D]
  ↓
回归头: [256D] → [1D] (放松度/力度)
```

**核心能力**:
- ✅ 实时感知 (ECG/EDA 动态信号)
- ✅ 跨模态融合 (动态 + 静态)
- ✅ 门控调制 (自适应重要性)
- ✅ 连续值预测 (放松度回归)

**局限性**:
- ❌ 依赖预训练的 TCM Encoder
- ❌ Gate 机制需要大量数据学习

---

## 2. 联动机制详解

### 2.1 数据流

```
[用户输入]
    ├─ ECG 信号 (实时) ──────────┐
    ├─ EDA 信号 (实时) ──→ Dynamic Encoder → pressure_embedding [128D]
    ├─ 舌象 (静态) ────────────┐
    ├─ 苔象 (静态) ──→ TCM Encoder → tcm_probs [9D] + tcm_internal [128D]
    ├─ 脉象 (静态) ────────────┘
    └─ 面象 (静态) ────────────┘

[门控调制]
Gate A: tcm_probs [9D] → σ(W_a [128×9]) → gate_a [128D]
  → modulated_pressure = pressure_embedding ⊙ gate_a

Gate B: pressure_embedding [128D] → σ(W_b [128×128]) → gate_b [128D]
  → modulated_static = tcm_internal ⊙ gate_b

[融合]
fused = [modulated_pressure, modulated_static] = [256D]

[预测]
output = Linear(256 → 128 → 1) → 放松度/力度值
```

### 2.2 控制流

```
前向传播:
  1. TCM Encoder (冻结, no_grad)
     → 提取静态体征的高维表征
     → 输出 9 种体质概率

  2. Dynamic Encoder (可训练)
     → 编码 ECG/EDA 信号
     → 输出 128D 动态表征

  3. Gate A (可训练)
     → 读取 TCM 概率
     → 生成动态特征的调制系数

  4. Gate B (可训练)
     → 读取动态表征
     → 生成静态特征的调制系数

  5. Fusion (可训练)
     → 拼接调制后的特征

  6. Regressor (可训练)
     → 输出连续值预测

反向传播:
  - TCM Encoder: 无梯度 (冻结)
  - Gate A/B + Fusion + Regressor: 正常更新
  - Dynamic Encoder: 正常更新
```

---

## 3. 消融实验: 评估主模型必要性

### 3.1 实验设计

我们需要对比以下模型变体:

| 变体 | TCM Encoder | Gate A | Gate B | 动态编码器 | 融合方式 | 参数量 |
|------|-------------|--------|--------|------------|----------|--------|
| **M0 (基线)** | ❌ | ❌ | ❌ | ✅ | 直接拼接 | ~2M |
| **M1 (仅 TCM)** | ✅ | ❌ | ❌ | ❌ | TCM → 回归 | ~200K |
| **M2 (仅动态)** | ❌ | ❌ | ❌ | ✅ | 动态 → 回归 | ~2M |
| **M3 (无 Gate)** | ✅ | ❌ | ❌ | ✅ | 简单拼接 | ~2.2M |
| **M4 (仅 Gate A)** | ✅ | ✅ | ❌ | ✅ | 部分门控 | ~2.2M |
| **M5 (仅 Gate B)** | ✅ | ❌ | ✅ | ✅ | 部分门控 | ~2.2M |
| **M6 (完整模型)** | ✅ | ✅ | ✅ | ✅ | 双门控 | ~2.2M |

### 3.2 评估指标

```
主要指标:
  - MAE (平均绝对误差): 回归任务核心指标
  - RMSE (均方根误差): 对大误差更敏感
  - R² (决定系数): 整体拟合度

次要指标:
  - 推理延迟 (ms)
  - 模型大小 (MB)
  - 训练速度 (samples/sec)

业务指标:
  - 方案推荐准确率
  - 力度等级匹配度
  - 用户满意度 (如果可用)
```

### 3.3 预期结果分析

#### 场景 1: 主模型显著优于其他变体

如果 M6 (完整模型) 的 MAE 比 M0-M5 低 **>15%**,则主模型存在必要性。

**可能原因**:
- Gate 机制确实学到了有意义的跨模态交互
- TCM 先验提供了独特的信息增益
- 动态信号补充了静态诊断的盲区

#### 场景 2: 主模型与 M3 (无 Gate) 相当

如果 M6 ≈ M3 (MAE 差异 <5%),则 Gate 机制可能无效。

**可能原因**:
- Gate 权重接近 1 (退化为简单拼接)
- 数据不足以学习复杂的门控模式
- 任务本身不需要复杂的跨模态交互

#### 场景 3: 主模型不如 M0 (基线)

如果 M6 < M0 (MAE 更高),则主模型可能过度设计。

**可能原因**:
- 过拟合 (参数量过大)
- 模态冲突 (TCM 和动态信号相互干扰)
- 训练不充分 (Gate 机制需要更多数据)

---

## 4. 当前状态评估

### 4.1 已完成的验证

从 `demo/test_demo.py` 的结果来看:

```
✅ 体质识别: 平和质 (78.27%)
✅ 推荐方案: 健脑强身
✅ 力度等级: 强劲
```

**问题**: 这只是推理测试,不是性能评估。

### 4.2 缺失的关键评估

我们需要:
- ❌ **对比实验**: M0 vs M1 vs M3 vs M6 的 MAE
- ❌ **消融研究**: Gate A vs Gate B 的单独贡献
- ❌ **案例分析**: 哪些样本受益于门控? 哪些被损害?
- ❌ **统计显著性**: 性能差异是否显著?

---

## 5. 决策框架

### 5.1 主模型存在的必要条件

主模型 (DualGatingModel) **有存在必要** 如果满足以下任意一条:

| 条件 | 验证方法 | 当前状态 |
|------|----------|----------|
| **性能显著优于简单融合** | M6 MAE < M3 MAE × 0.85 | ❓ 未验证 |
| **TCM 先验有信息增益** | M6 MAE < M2 MAE × 0.95 | ❓ 未验证 |
| **Gate 机制可解释** | Gate 权重与中医理论一致 | ⚠️ 部分验证 |
| **业务需求要求可解释性** | Gate 权重可用于解释决策 | ✅ 满足 |
| **推理延迟可接受** | P95 延迟 < 100ms | ❓ 未验证 |

### 5.2 简化模型的触发条件

如果以下条件 **全部满足**,考虑简化:

- ✅ M6 MAE ≤ M0 MAE × 1.05 (差异 <5%)
- ✅ M6 推理延迟 > M0 两倍
- ✅ Gate 权重接近常数 (σ ≈ 1)
- ✅ 业务不需要可解释性

**简化方案**: 
- 移除 Gate A/B
- 移除 TCM Encoder
- 使用简单的动态信号回归模型

---

## 6. 建议的下一步行动

### 立即行动 (本周)

1. **运行消融实验**
   ```bash
   # 创建实验脚本
   python experiments/ablation_study.py \
     --data company_data \
     --models M0 M1 M2 M3 M6 \
     --metrics MAE RMSE R2
   ```

2. **可视化 Gate 权重**
   - 分析 Gate A/B 在不同样本上的变化
   - 检查是否有可解释的模式

3. **延迟测试**
   ```python
   import time
   start = time.time()
   # 推理 1000 次
   latency = (time.time() - start) / 1000
   ```

### 短期行动 (2 周)

4. **案例研究**
   - 找出 10 个高误差样本
   - 分析 Gate 机制是否帮助或损害

5. **超参数敏感性分析**
   - Gate 权重初始化 (零 vs 随机)
   - Fusion 维度 (128 vs 256)
   - Dropout 比例

### 中期行动 (1 个月)

6. **A/B 测试** (如果有在线流量)
   - 对照组: 简单模型
   - 实验组: 完整模型
   - 评估业务指标

---

## 7. 结论

### 当前认知

**主模型存在的理由**:
1. ✅ **架构完整性**: Gate A/B 提供了理论上的跨模态交互能力
2. ✅ **可解释性**: Gate 权重可用于解释决策
3. ✅ **预训练迁移**: TCM Encoder 带来了领域知识
4. ❓ **性能增益**: **未经验证**

**主模型的风险**:
1. ⚠️ **过拟合**: Gate 机制需要大量数据
2. ⚠️ **复杂度**: 维护成本高,调试困难
3. ⚠️ **延迟**: 两个编码器串行推理

### 最终判断

**主模型有存在的潜力,但需要实验验证其必要性。**

如果消融实验显示:
- **M6 显著优于 M3** (MAE 低 >15%) → 主模型值得保留
- **M6 ≈ M3** (MAE 差异 <5%) → Gate 机制可能多余
- **M6 < M0** → 过度设计,需要简化

**建议**: 在获得公司数据后立即运行消融实验,用数据说话。
