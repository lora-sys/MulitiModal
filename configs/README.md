# Configurations

This directory will contain explicit, reproducible training and inference configuration. It must not contain credentials, company data, or fallback chains that guess machine-specific paths.

